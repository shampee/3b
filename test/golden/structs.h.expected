// Generated by 3b -- do not edit by hand.
#ifndef BBB_PKG_GOLDEN_H
#define BBB_PKG_GOLDEN_H

#include "3b_runtime.h"

// --- alias (typedef) declarations -------------------------------------
// --- enum / flags declarations ----------------------------------------
// --- struct declarations ---------------------------------------------
typedef struct golden_Inner golden_Inner;
typedef struct golden_Outer golden_Outer;

typedef struct golden_Inner {
  i32 depth;
} golden_Inner;

static inline b32 golden_Inner_eq(golden_Inner a, golden_Inner b) {
  return (a.depth == b.depth);
}

static inline i32 golden_Inner_cmp(golden_Inner a, golden_Inner b) {
  i32 c;
  c = ((a.depth < b.depth) ? -1 : (a.depth > b.depth) ? 1 : 0); if (c != 0) return c;
  return 0;
}

typedef struct golden_Outer {
  bbb_String8 name;
  golden_Inner inner;
} golden_Outer;

static inline b32 golden_Outer_eq(golden_Outer a, golden_Outer b) {
  return bbb_str8_match(a.name, b.name, 0)
      && golden_Inner_eq(a.inner, b.inner);
}

static inline i32 golden_Outer_cmp(golden_Outer a, golden_Outer b) {
  i32 c;
  c = bbb_str8_compare(a.name, b.name); if (c != 0) return c;
  c = golden_Inner_cmp(a.inner, b.inner); if (c != 0) return c;
  return 0;
}

// --- handle pool declarations ------------------------------------------
// --- public global declarations ---------------------------------------

// --- public function prototypes ---------------------------------------
golden_Outer golden_make(void);
i32 golden_depth_of(golden_Outer o);
int main(int argc, char** argv);

#endif
