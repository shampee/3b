// Generated by 3b -- do not edit by hand.
#include "golden.h"
#include "3b_runtime.h"

// --- alias (typedef) declarations -------------------------------------
// --- enum / flags declarations ----------------------------------------
// --- struct declarations ---------------------------------------------

// --- handle pool declarations ------------------------------------------
// --- global variable forward declarations ------------------------------

// --- function prototypes ----------------------------------------------

golden_Plain golden_widen(golden_Plain n, golden_Plain m) {
  return (n + m);
}

golden_Pinned golden_scale(golden_Pinned n, golden_Pinned by) {
  return (n * by);
}

void golden_fill(golden_Pinned* out, golden_Pinned n, golden_Pinned by) {
  (out[0] = golden_scale(n, by));
}

int main(int _3b_argc, char** _3b_argv) {
  bbb_Context _3b_ctx;
  bbb_ctx_init(&_3b_ctx, bbb_MB(16));
  golden_Pinned slot = 3LL;;
  golden_Pinned by = 5LL;;
  golden_fill(&slot, slot, by);
  golden_Plain grown = golden_widen(slot, slot);;
  bbb_ctx_free();
  return (i32)(grown + slot);
}

