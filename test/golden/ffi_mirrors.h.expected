// Generated by 3b -- do not edit by hand.
#ifndef BBB_PKG_GOLDEN_H
#define BBB_PKG_GOLDEN_H

#include "3b_runtime.h"

// --- alias (typedef) declarations -------------------------------------
// --- enum / flags declarations ----------------------------------------
typedef enum {
  golden_Status_Ok = 0,
  golden_Status_Err = 1,
  golden_Status_Count = 2,
} golden_Status;

// --- struct declarations ---------------------------------------------
typedef struct golden_Doc golden_Doc;

typedef struct golden_Doc {
  i64 size;
} golden_Doc;

static inline b32 golden_Doc_eq(golden_Doc a, golden_Doc b) {
  return (a.size == b.size);
}

static inline i32 golden_Doc_cmp(golden_Doc a, golden_Doc b) {
  i32 c;
  c = ((a.size < b.size) ? -1 : (a.size > b.size) ? 1 : 0); if (c != 0) return c;
  return 0;
}

// --- handle pool declarations ------------------------------------------
// --- public global declarations ---------------------------------------

// --- public function prototypes ---------------------------------------
golden_Status golden_load(golden_Doc* doc, void** out);
const char* golden_name_of(golden_Doc** docs);
golden_Status golden_peek(golden_Doc* doc);
int main(int argc, char** argv);

#endif
